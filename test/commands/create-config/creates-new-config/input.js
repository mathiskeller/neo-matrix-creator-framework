'use strict';

const input = ({ keys }) => [
  '192.168.2.23',
  keys.enter,

  'n',
  keys.enter,

  keys.down,
  keys.enter,

  keys.enter,
  '123',
  keys.enter,
  keys.enter,
  keys.enter,
  keys.enter,
  keys.enter,
  keys.enter,
  keys.enter
];

module.exports = input;
